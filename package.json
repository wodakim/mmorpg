{
  "name": "maestria-pixel",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "client",
    "server",
    "shared"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c green,blue \"npm run dev --workspace server\" \"npm run dev --workspace client\"",
    "dev:client": "npm run dev --workspace client",
    "dev:server": "npm run dev --workspace server",
    "typecheck": "npm run build:types --workspace @maestria/shared && npm run typecheck --workspace @maestria/shared && npm run typecheck --workspace @maestria/server && npm run typecheck --workspace @maestria/client"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "typescript": "^5.9.2"
  }
}
